generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Notification {
  id        String   @id @default(uuid())
  title     String
  recipient String
  message   String
  createdAt DateTime @default(now())
}
